[project]
name = "fly_in"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
	"colorama"
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pydantic"
]

lint = [
	"flake8>=7.3.0",
	"mypy>=2.1.0",
]

[tool.uv]
default-groups = ["dev", "lint"]